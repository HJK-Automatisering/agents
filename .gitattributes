# Vi udvikler på Windows, og hooks og workflows køres på Linux.
# LF i repoet uanset hvilken platform der committer.
* text=auto eol=lf

# PowerShell-scripts køres på Windows. Git Bash og PowerShell klarer begge LF,
# så de følger reglen ovenfor — men markér dem som tekst, så de aldrig
# behandles som binære ved en fejl.
*.ps1 text
*.md  text
*.json text
*.yaml text
*.yml  text
