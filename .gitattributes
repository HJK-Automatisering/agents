# Vi udvikler på Windows, og hooks og workflows køres på Linux.
# LF i repoet uanset hvilken platform der committer.
* text=auto eol=lf

*.md  text
*.json text
*.yaml text
*.yml  text

# Node-hooks. LF på alle platforme.
*.cjs text eol=lf
